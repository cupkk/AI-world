global:
  resolve_timeout: 5m

route:
  receiver: default
  group_by: ['alertname', 'severity']
  group_wait: 15s
  group_interval: 5m
  repeat_interval: 2h

receivers:
  - name: default
    webhook_configs: []
