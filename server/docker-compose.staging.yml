services:
  postgres:
    image: pgvector/pgvector:pg16
    restart: unless-stopped
    env_file:
      - .env.staging
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-aiworld}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-aiworld_dev}
      POSTGRES_DB: ${POSTGRES_DB:-aiworld}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${POSTGRES_USER:-aiworld} -d ${POSTGRES_DB:-aiworld}",
        ]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: redis-server --appendonly yes --maxmemory 512mb --maxmemory-policy allkeys-lru
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  api:
    image: ${REGISTRY:?Missing REGISTRY}/aiworld-api:${IMAGE_TAG:?Missing IMAGE_TAG}
    restart: unless-stopped
    env_file:
      - .env.staging
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - uploads:/app/uploads
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://localhost:3000/ready || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 30s
    networks:
      default:
      edge:
        aliases:
          - staging-api
      observability:
        aliases:
          - staging-api

  worker:
    image: ${REGISTRY:?Missing REGISTRY}/aiworld-worker:${IMAGE_TAG:?Missing IMAGE_TAG}
    restart: unless-stopped
    env_file:
      - .env.staging
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - uploads:/app/uploads

  web:
    image: ${REGISTRY:?Missing REGISTRY}/aiworld-web:${IMAGE_TAG:?Missing IMAGE_TAG}
    restart: unless-stopped
    depends_on:
      - api
    ports:
      - ${STAGING_WEB_BIND:-127.0.0.1:18080:80}
    networks:
      edge:
        aliases:
          - staging-web

volumes:
  pgdata:
  redisdata:
  uploads:

networks:
  edge:
    external: true
    name: aiworld-edge
  observability:
    external: true
    name: aiworld-observability
