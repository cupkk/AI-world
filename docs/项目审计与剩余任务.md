# 项目审计与剩余任务

更新时间：2026-03-18

## 1. 当前结论

AI-World 已完成本轮“封板上线”收尾，可以进入正式对外使用阶段。

当前结论：

- 上线阻断项：无
- 部署模型：单服务器 production-only
- 线上地址：`https://ai-world.asia`
- 服务器：`47.238.143.212`
- 当前发布镜像 tag：`20260317-publish-manage`

本轮之后不再继续扩需求；后续事项只保留上线后运维和安全跟进。

## 2. 已完成的封板事项

### 2.1 架构与发布链路收口

已完成：

- 删除仓库中的 `staging` 发布工作流与运行栈定义
- 运维脚本统一为 production-only
- Nginx 只保留生产域名与生产 upstream
- Prometheus 只抓取生产 API
- 运维手册、上线清单、ops runbook 已同步改为单服务器模型

### 2.2 生产安全收口

已完成：

- 生产 `SESSION_SECRET` 已替换为强随机值
- `ENABLE_DEMO_INVITES=false`
- `ENABLE_PUBLIC_SAMPLE_INVITES=false`
- `PUBLIC_SAMPLE_INVITES=` 清空
- `ENABLE_ASSISTANT=false`
- `ENABLE_KNOWLEDGE_BASE=false`
- 前端生产构建已显式注入对应 feature flags
- `invite/login` 页面不再在生产环境回退显示公开样例邀请码

### 2.3 功能封板

本轮封板后，当前对外主能力包括：

- 邀请码注册 / 登录 / 找回密码
- Learner / Expert / Enterprise / Admin 四角色基础工作台
- 内容发布、审核、驳回、重提交流程
- 作者可在“我的内容”中修改或删除自己已发布的内容
- Hub / Profile / Publish / Dashboard 聚合读取模型
- 申请系统 `outbox / inbox / audit`
- 管理端 Review、内容治理、审计日志
- 站内消息与 WebSocket 基础能力

本轮默认关闭：

- Assistant
- Knowledge Base

这两个模块不是删除，而是显式封闭，待后续单独验收后再开。

## 3. 2026-03-17 实际上线动作

已完成：

- 连接生产服务器 `47.238.143.212`
- 备份远端 `server/.env.production`
- 将当前本地仓库同步到 `/opt/aiworld`
- 在服务器构建并部署镜像：
  - `ghcr.io/cupkk/ai-world/aiworld-api:20260317-seal`
  - `ghcr.io/cupkk/ai-world/aiworld-web:20260317-seal`
  - `ghcr.io/cupkk/ai-world/aiworld-worker:20260317-seal`
- 重建 edge / observability 容器
- 关闭并移除服务器上的 staging 运行栈
- 删除远端 `server/docker-compose.staging.yml`
- 删除远端 `server/.env.staging`

## 4. 线上验收结果

已确认：

- `https://ai-world.asia/ready` 返回 `200`
- `https://ai-world.asia/health` 返回 `200`
- `https://ai-world.asia/api/auth/invite/samples` 返回空数组
- `./server/scripts/check-stack.sh` 通过
- `./server/scripts/verify-observability.sh` 通过
- 当前运行中只剩 production / edge / observability 容器

## 5. 当前仍需处理的事项

以下事项存在，但不再阻断本次上线：

### P1：上线后 24 小时内完成

- 轮换 SMTP 凭据
- 轮换 OSS 凭据
- 轮换 LLM API Key

原因：

- 这些生产凭据当前仍在服务器环境文件中明文存在
- 从安全角度，应该在本次封板后尽快完成轮换

### P1：上线后补完整体冒烟

- 使用正式的管理员、学员、专家、企业账号执行一次 `npm run test:live:prod-smoke`

说明：

- 当前已完成公开接口、健康检查、栈检查、观测检查
- 但完整登录态 smoke 仍需要真实账号环境变量

### P2：后续优化，不属于本次封板

- Assistant 会话持久化、引用来源、反馈闭环
- Knowledge Base 处理链路观测、失败重试、任务排障
- 审计日志分页、更多筛选维度、导出能力
- 更细的后台治理联动和更多 E2E 变体覆盖

这些属于下一阶段迭代，不再纳入本次上线收尾。

## 6. 封板结论

本项目现在已经从“继续打磨中的开发态”切到“可上线使用的生产态”。

本次封板之后，接下来的工作原则只有两条：

1. 不再新增上线前需求。
2. 只处理真实线上故障、安全轮换和用户反馈。

## 7. 2026-03-17 管理员与数据清空收口

已完成：

- 新增独立管理员用户后台：`/admin/users`
- 支持可视化查看全量用户数据，包括角色、状态、邀请码、内容数、知识库文件数、申请数、创建时间、最近登录等
- 支持在后台直接暂停 / 恢复用户账号
- 生产服务已切换到 tag：`20260317-admin-finalize`
- 生产数据库已提前备份，备份文件：`/root/backups/aiworld-pre-reset-20260317-admin-finalize.dump`
- 生产 `public` 业务表已清空，仅保留 `_prisma_migrations`
- Redis 会话 / 缓存已清空
- uploads 用户上传目录已清空
- 生产库当前仅保留 1 个管理员账号：`1810365936@qq.com`
- 该账号状态：`ADMIN / active`

管理员账号信息：

- Email：`1810365936@qq.com`
- Password：`a1810365936`
- 后台入口：
  - `https://ai-world.asia/admin/review`
  - `https://ai-world.asia/admin/hub`
  - `https://ai-world.asia/admin/users`

GitHub Actions 结论：

- 保留 `CI`：用于类型检查与基础回归，仍然有价值
- `Deploy Staging`：已无实际用途，已删除
- `Promote Production`：已无实际用途，已删除
- 当前发布方式为单服务器直连构建 + 文件同步 + 服务器本地更新

## 8. 2026-03-18 本轮收口进展

已完成：

- 人才库补齐更完整的检索与筛选：关键词、地点、机构、角色、意向标签、技能标签、排序
- Talent 页面已接入新的筛选 UI，与后端查询参数对齐
- 私信治理闭环补齐：消息请求、拉黑、解除拉黑、举报用户、举报消息、举报会话
- 后台举报审核卡片已增强上下文，能看到举报人、被举报用户、消息摘要、会话参与人
- 联系方式设置页已支持查看和管理已拉黑用户
- Hub 页面已增加更系统化的内容导航层：`课程层 / 资源层 / 工具层`
- 本轮代码相关的定向测试已补齐，Talent 搜索测试与会话举报测试已同步更新

当前不再新增上线前功能。剩余事项仅保留：

- 生产凭据轮换：SMTP / OSS / LLM API Key
- 上线后根据真实用户反馈做缺陷修复，不再做范围扩张

## 9. 2026-03-18 生产同步与最终冒烟

已完成：

- 当前生产代码已同步到服务器 `/opt/aiworld`
- 服务器本地构建并切换到发布 tag：`20260318-governance-finalize`
- 当前运行镜像：
  - `ghcr.io/cupkk/ai-world/aiworld-api:20260318-governance-finalize`
  - `ghcr.io/cupkk/ai-world/aiworld-web:20260318-governance-finalize`
  - `ghcr.io/cupkk/ai-world/aiworld-worker:20260318-governance-finalize`
- `./server/scripts/check-stack.sh` 通过
- `./server/scripts/verify-observability.sh` 通过
- `https://ai-world.asia/ready` 返回 `200`
- `https://ai-world.asia/health` 返回 `200`
- `https://ai-world.asia/api/auth/invite/samples` 返回空数组
- 最终线上 Playwright 冒烟通过：`5/5 passed`

本次同步中已确认：

- 生产前端 `.env.production` 当前实际开启了 `Assistant / Knowledge Base`
- 生产后端 `.env.production` 已与前端开关重新统一
- 已补齐 3 个内部烟测账号用于 Learner / Expert / Enterprise 角色回归

截至当前，发布阻断项已清空。剩余事项只保留：

- 轮换生产凭据：`SMTP / OSS / LLM API Key`
- 继续观察线上真实用户反馈与运行日志

## 10. 2026-03-18 生产凭据同步补充

已完成：

- 已将本地 `server/.env.local` 同步到生产服务器 `server/.env.local`
- 已将新的 `LLM_API_KEY` 同步到生产 `server/.env.production`
- 已重建生产 `api / worker` 容器，并确认 `https://ai-world.asia/ready` 返回 `200`
- 已确认生产 `api / worker` 实际加载到的新 `LLM_API_KEY` 与本地一致
- 已修复 `server/scripts/bootstrap-runtime-envs.py` 的一个生产回退问题：
  - 当服务器缺少 `server/.env` 时，脚本现在会优先回退到现有 `server/.env.production`
  - 避免再次错误覆盖 `POSTGRES_* / REDIS_URL` 等生产关键配置

当前仍未完成：

- `SMTP` 仍是旧的 `QQ SMTP`，尚未切换到阿里云 `DirectMail`
- `OSS` 仍是旧的 `AK/SK`，尚未切换到新的独立 `RAM 用户` 凭据

阻塞项：

- 需要阿里云 `DirectMail` 的新 SMTP 参数：`MAIL_FROM / SMTP_HOST / SMTP_PORT / SMTP_USER / SMTP_PASS`
- 需要新的 OSS `RAM` 凭据：`OSS_ACCESS_KEY_ID / OSS_ACCESS_KEY_SECRET`

结论：

- 本次已完成 `LLM API Key` 的生产同步与生效验证
- `SMTP / OSS` 轮换还差阿里云控制台侧新凭据，拿到后可直接继续切换，不需要改业务代码

## 11. 2026-03-18 SMTP / OSS 最终切换验证

已完成：

- 已将本地 `.env.local` 中的阿里云 `DirectMail` SMTP 参数同步到生产 `server/.env.production`
- 已将新的 OSS `RAM` 凭据同步到生产 `server/.env.production`
- 已重建生产 `api / worker` 容器，并再次确认 `https://ai-world.asia/ready` 返回 `200`
- 已确认生产 `api` 实际加载：
  - `SMTP_HOST=smtpdm.aliyun.com`
  - `SMTP_USER=noreply@mail.ai-world.asia`
  - 新的 `OSS_ACCESS_KEY_ID`
  - `LLM_PROVIDER=qwen`

实测结果：

- SMTP 实发测试通过：
  - 收件人：`1810365936@qq.com`
  - 结果：`accepted`
  - 发件域名：`mail.ai-world.asia`
- OSS 上传 / 删除测试通过：
  - `PUT` 返回 `200`
  - `DELETE` 返回 `204`
- LLM Embedding 调用测试通过：
  - Qwen 兼容接口返回 `200`
  - 向量维度：`1536`
- 最近日志中未发现新的 `SMTP / OSS / LLM` 鉴权失败

当前结论：

- `SMTP / OSS / LLM API Key` 三项生产凭据已完成切换并验证
- 当前上线收尾阶段剩余事项只保留：
  - 继续观察真实用户反馈与运行日志
  - 如旧 `QQ SMTP` 授权码和旧 OSS `AK/SK` 仍在阿里云 / QQ 控制台中可用，立即手动作废
